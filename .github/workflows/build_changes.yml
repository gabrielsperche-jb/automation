name: List changed directories

on: [push]

jobs:
  list-changed-directories:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Check if directory exists
        run: echo "O caminho do repositório é ${{ github.workspace }}"
