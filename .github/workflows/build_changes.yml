name: Test

on: [push]

jobs:
  listar-diretorios-modificados:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Listar diretórios modificados
        run: |
          echo "Thats all changes ${{ git diff --name-only HEAD }}"
